package Stack;
import java.util.* ;
public class LiveClass {
    public static void main(String[] args) {
        Stack<Integer> st = new Stack<>() ;
    /*    //stack push will push inside to stack

        //stack pop will remove the last first element from top of stack ;
        //stack will remove the last value
  //      st.pop() ;
 //       System.out.println(st);
        //stack peek will give the last element in stack
        //it just gives the top element,but it will not remove
        int el = st.peek() ;
        System.out.println(el);*/



    }
}
